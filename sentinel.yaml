version: "v1"
sentinel:
  app:
    type: 0
  log:
    pid: false
    metric:
      maxFileCount: 14
      flushIntervalSec: 1
      # 100MB
      singleFileMaxSize: 104857600
  stat:
    system:
      collectIntervalMs: 1000